# AKShare MCP Server Environment Variables

# Python executable path (optional, defaults to 'python')
# PYTHON_PATH=python

# Log level (optional, defaults to 'info')
# LOG_LEVEL=debug

# Request timeout in seconds (optional, defaults to 30)
# REQUEST_TIMEOUT=30

# Cache results in seconds (optional, defaults to 0 = no caching)
# CACHE_TTL=0